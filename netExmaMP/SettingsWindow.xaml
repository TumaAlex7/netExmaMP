<Window x:Class="netExmaMP.SettingsWindow" x:Name="Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:netExmaMP"
        mc:Ignorable="d"
        Title="SettingsWindow" Height="450" Width="400">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="{Binding ElementName=Window, Path=Background}"/>
            <Setter Property="BorderBrush" Value="{Binding ElementName=Window, Path=Foreground}"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/> 
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="AUTO"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Label Content="Language" Foreground="{Binding ElementName=Window, Path=Foreground}"/>
            <ComboBox Grid.Column="1" x:Name="lang">
                
            </ComboBox>

            <Label Grid.Row="1" Content="Foreground color" Foreground="{Binding ElementName=Window, Path=Foreground}"/>
            <TextBox x:Name="foreground" Grid.Row="1" Grid.Column="1"/>

            <Label Grid.Row="2" Content="Background color" Foreground="{Binding ElementName=Window, Path=Foreground}"/>
            <TextBox x:Name="background" Grid.Row="2" Grid.Column="1"/>

            <Label Grid.Row="3" Content="Manual text color input" Foreground="{Binding ElementName=Window, Path=Foreground}"/>
            <CheckBox x:Name="autotext" Grid.Row="3" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,13,0"/>

            <Label Grid.Row="4" Content="Text color" Foreground="{Binding ElementName=Window, Path=Foreground}" IsEnabled="{Binding ElementName=autotext, Path=IsChecked}"/>
            <TextBox x:Name="textcolor" Grid.Row="4" Grid.Column="1" IsEnabled="{Binding ElementName=autotext, Path=IsChecked}"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button x:Name="CancelBtn" Content="Cancel" Margin="3" Click="CancelBtn_Click" Foreground="{Binding ElementName=Window, Path=Foreground}"/>
            <Button x:Name="SaveBtn" Grid.Column="1" Content="Save" Margin="3" Click="SaveBtn_Click" Foreground="{Binding ElementName=Window, Path=Foreground}"/>
            <Button x:Name="ApplyBtn" Grid.Column="2" Content="Apply" Margin="3" Click="ApplyBtn_Click" Foreground="{Binding ElementName=Window, Path=Foreground}"/>
        </Grid>
    </Grid>
</Window>
