<Window x:Class="netExmaMP.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:netExmaMP"
        mc:Ignorable="d"
        Title="SettingsWindow" Height="450" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/> 
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="AUTO"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Label Content="Language"/>
            <ComboBox Grid.Column="1" x:Name="lang">
                
            </ComboBox>

            <Label Grid.Row="1" Content="Foreground color"/>
            <TextBox x:Name="foreground" Grid.Row="1" Grid.Column="1"/>

            <Label Grid.Row="2" Content="Background color"/>
            <TextBox x:Name="background" Grid.Row="2" Grid.Column="1"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button x:Name="CancelBtn" Content="Cancel" Margin="3" Click="CancelBtn_Click"/>
            <Button x:Name="SaveBtn" Grid.Column="1" Content="Save" Margin="3" Click="SaveBtn_Click"/>
            <Button x:Name="ApplyBtn" Grid.Column="2" Content="Apply" Margin="3" Click="ApplyBtn_Click"/>
        </Grid>
    </Grid>
</Window>
